[pytest]
testpaths = tests
norecursedirs = fixtures .venv .git
addopts = -ra --strict-markers
markers =
    live: opt-in test using an actual agent or model service
    profile(name): execution profile required by a live test
